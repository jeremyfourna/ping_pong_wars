dependencies:
  cache_directories:
    - ~/.meteor
  override:
    - meteor || curl https://install.meteor.com | /bin/sh
    - npm install -g gagarin
    - npm install -g eslint

test:
  override:
    - gagarin -v
    - eslint client/ lib/ server/

notify:
  webhooks:
    # A list of hook hashes, containing the url field
    # gitter hook
    - url: https://webhooks.gitter.im/e/d187e252a0149d4693da